requests
python-dotenv
pandas
numpy
scipy
scikit-learn
matplotlib

# xgboost is OPTIONAL and deliberately not pinned here.
#
# model.py and forecast.py try to import XGBClassifier and fall back to
# sklearn's HistGradientBoostingClassifier when it is missing, so the pipeline
# and CI run either way. Wheels for the newest Python releases often lag by a
# few months, and a hard dependency would break the build for a model that has
# not yet beaten a one-line baseline anyway.
#
# To use it:  pip install xgboost
