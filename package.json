{
  "name": "whatsgoingon",
  "version": "0.1.0",
  "private": true,
  "description": "Conflict risk analyzer: ACLED political violence data overlaid on a portfolio of index and sector ETFs.",
  "scripts": {
    "dev": "next dev",
    "prebuild": "node scripts/check-artifacts.mjs",
    "build": "next build",
    "start": "next start",
    "lint": "next lint",
    "check": "node scripts/check-artifacts.mjs",
    "typecheck": "tsc --noEmit",
    "test:chain": "bun test lib/chain.test.ts",
    "test:py": "cd research && python -m unittest discover tests"
  },
  "dependencies": {
    "maplibre-gl": "^5.0.0",
    "next": "^15.1.0",
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "@types/react": "^19.0.0",
    "@types/react-dom": "^19.0.0",
    "typescript": "^5.7.0"
  },
  "engines": {
    "node": ">=20"
  }
}
